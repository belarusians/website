version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use && npm ci
    build:
      commands:
        - npm --version && npm run build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
